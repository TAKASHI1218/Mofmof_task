<%= form_for @building do |f| %>
  <% if @building.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(building.errors.count, "error") %> prohibited this building from being saved:</h2>

      <ul>
      <% building.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :rent %>
    <%= f.number_field :rent %>円
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_area :address %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>年
  </div>

  <div class="field">
    <%= f.label :info %>
    <%= f.text_area :info %>
  </div>

<%= f.fields_for :station ,(@station) do |station| %>
<%#= f.fields_for :stations do |station| %>


  <div class="field">
    <%= station.label :index_number %>
    <%= station.number_field :index_number %>
  </div>

  <div class="field">
    <%= station.label :line_name %>
    <%= station.text_area :line_name %>
  </div>

  <div class="field">
   <%= station.label :station_name %>
   <%= station.text_area :station_name %>
 </div>

  <div class="field">
    <%= station.label :how_long %>
    <%= station.number_field :how_long %>分
  </div>
<% end %>


 <div class="actions">
  <%= f.submit %>
</div>
<% end %>
